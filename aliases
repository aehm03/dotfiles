# Aliases to protect against overwriting
alias cp='cp -i'
alias mv='mv -i'

# Enable aliases for some commands
alias sudo='sudo '
alias watch='watch '

# Search kubernetes history
alias khist="history | grep '^ \d*\s*k.*' --color=never"
